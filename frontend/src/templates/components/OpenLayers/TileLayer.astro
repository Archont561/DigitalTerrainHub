---
export interface Props extends Astro.WebComponentsProps.OlTileLayer {};
---

<ol-tile-layer {...Astro.props} ><slot /></ol-tile-layer>

<script>
    import { HTMLOlTileLayer } from "@lib/WebComponents";
    (() => {
        const tag = "ol-tile-layer";
        customElements.whenDefined(tag).then(() => {
            console.log(`Defined ${tag}:${HTMLOlTileLayer.name} web component!`);
        });
    })();
</script>