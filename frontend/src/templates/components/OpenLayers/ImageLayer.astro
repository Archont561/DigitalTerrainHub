---
export interface Props extends Astro.WebComponentsProps.OlImageLayer {};
---

<ol-image-layer {...Astro.props} ><slot /></ol-image-layer>

<script >
    import { HTMLOlImageLayer } from "@lib/WebComponents";
    (() => {
        const tag = "ol-image-layer";
        customElements.whenDefined(tag).then(() => {
            console.log(`Defined ${tag}:${HTMLOlImageLayer.name} web component!`);
        });
    })();
</script>