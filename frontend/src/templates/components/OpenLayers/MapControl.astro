---
export interface Props extends Astro.WebComponentsProps.OLMapControl {};
---

<ol-map-control {...Astro.props} ><slot /></ol-map-control>

<script>
    import { HTMLOlMapControl } from "@lib/WebComponents";
    (() => {
        const tag = "ol-map-control";
        customElements.whenDefined(tag).then(() => {
            console.log(`Defined ${tag}:${HTMLOlMapControl.name} web component!`);
        });
    })();
</script>