---
export interface Props extends Astro.WebComponentsProps.OlMap {};
---

<ol-map {...Astro.props} ><slot /></ol-map>

<script >
    import { HTMLOlMap } from "@lib/WebComponents";
    (() => {
        const tag = "ol-map";
        customElements.whenDefined(tag).then(() => {
            console.log(`Defined ${tag}:${HTMLOlMap.name} web component!`);
        });
    })();
</script>